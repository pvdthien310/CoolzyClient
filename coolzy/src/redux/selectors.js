export const currentUser = (state) => state.account.user
export const currentStaffList = (state) => state.staff.staffList
export const currentListItem = (state) => state.checkout.listItems
export const isOrderFromCart = (state) => state.checkout.isFromCart